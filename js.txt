1) Design web pages for College website using following tags: 1. Home page: Elements, Attribute, Head, Body, Hyperlink. 2. Department: Table tag. 3. Facility: Ordered or unordered list tag. 4. Contact us: Form, Textbox, button, TextArea.

<!DOCTYPE html>
<html>
<head><title>College Website</title></head>
<body>

<h1>My College</h1>
<a href="#home">Home</a> |
<a href="#dept">Department</a> |
<a href="#facility">Facilities</a> |
<a href="#contact">Contact Us</a>

<!-- Home -->
<h2 id="home">Welcome to My College</h2>
<p>Explore our departments, facilities, and get in touch with us.</p>

<!-- Department -->
<h2 id="dept">Departments</h2>
<table border="1">
<tr><th>Name</th><th>HOD</th></tr>
<tr><td>CSE</td><td>Dr. Rao</td></tr>
<tr><td>ECE</td><td>Dr. Mehta</td></tr>
</table>

<!-- Facilities -->
<h2 id="facility">Facilities</h2>
<ul>
<li>Library</li>
<li>Sports Complex</li>
<li>Cafeteria</li>
</ul>

<!-- Contact Us -->
<h2 id="contact">Contact Us</h2>
<form>
Name: <input type="text"><br>
Email: <input type="text"><br>
Message:<br><textarea></textarea><br>
<button type="submit">Send</button>
</form>

</body>
</html>

2) Design the web page for online book store using CSS3

<!DOCTYPE html>
<html>
<head>
<title>Online Book Store</title>
<style>
body {font-family: sans-serif; text-align:center; background:#f4f4f4;}
h1 {color:#333;}
.book {display:inline-block; background:#fff; padding:10px; margin:10px; border-radius:8px; box-shadow:0 0 5px #aaa;}
button {background:#007bff; color:#fff; border:none; padding:5px 10px; border-radius:5px; cursor:pointer;}
button:hover {background:#0056b3;}
</style>
</head>
<body>

<h1>Online Book Store</h1>

<div class="book">
<h3>Python Basics</h3>
<p>₹299</p>
<button>Buy</button>
</div>

<div class="book">
<h3>AI with ML</h3>
<p>₹499</p>
<button>Buy</button>
</div>

<div class="book">
<h3>Web Development</h3>
<p>₹399</p>
<button>Buy</button>
</div>

</body>
</html>


3) Design the web page for online book store using bootstrapping.

<!DOCTYPE html>
<html lang="en">
<head>
<title>Online Book Store</title>
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light text-center p-3">

<h1 class="mb-4 text-primary">Online Book Store</h1>

<div class="container">
  <div class="row">
    <div class="col-md-4 mb-3">
      <div class="card p-3">
        <h5>Python Basics</h5>
        <p>₹299</p>
        <button class="btn btn-primary">Buy</button>
      </div>
    </div>
    <div class="col-md-4 mb-3">
      <div class="card p-3">
        <h5>AI with ML</h5>
        <p>₹499</p>
        <button class="btn btn-primary">Buy</button>
      </div>
    </div>
    <div class="col-md-4 mb-3">
      <div class="card p-3">
        <h5>Web Development</h5>
        <p>₹399</p>
        <button class="btn btn-primary">Buy</button>
      </div>
    </div>
  </div>
</div>

</body>
</html>

4) Write JavaScript to validate the following fields of the Registration page. 1. First Name (Name should contains alphabets and the length should not be less than 6 characters). 2. Password (Password should not be less than 6 characters length). 3. E-mail id (should not contain any invalid and must follow the standard pattern name@domain.com) 4. Mobile Number (Phone number should contain 10 digits only). Last Name and Address (should not be Empty)

<!DOCTYPE html>
<html>
<head>
  <title>Registration Form</title>
</head>
<body>
  <h2>Registration Form</h2>

  <form onsubmit="return validateForm()">
    First Name: <input type="text" id="fname"><br><br>
    Last Name: <input type="text" id="lname"><br><br>
    Address: <input type="text" id="addr"><br><br>
    Password: <input type="password" id="pass"><br><br>
    Email: <input type="text" id="email"><br><br>
    Mobile: <input type="text" id="mob"><br><br>
    <button type="submit">Register</button>
  </form>

  <script>
    function validateForm() {
      // get input values
      let fname = document.getElementById("fname").value;
      let lname = document.getElementById("lname").value;
      let addr = document.getElementById("addr").value;
      let pass = document.getElementById("pass").value;
      let email = document.getElementById("email").value;
      let mob = document.getElementById("mob").value;

      // 1. First name: only letters, min 6 chars
      if (!/^[A-Za-z]{6,}$/.test(fname)) {
        alert("First Name must contain only letters and at least 6 characters.");
        return false;
      }

      // 2. Password: min 6 chars
      if (pass.length < 6) {
        alert("Password must be at least 6 characters long.");
        return false;
      }

      // 3. Email: simple pattern check
      if (!/^[\w.-]+@[A-Za-z]+\.[A-Za-z]{2,3}$/.test(email)) {
        alert("Enter a valid email (like name@example.com).");
        return false;
      }

      // 4. Mobile: exactly 10 digits
      if (!/^\d{10}$/.test(mob)) {
        alert("Mobile number must have exactly 10 digits.");
        return false;
      }

      // 5. Last Name and Address: not empty
      if (lname.trim() === "" || addr.trim() === "") {
        alert("Last Name and Address cannot be empty.");
        return false;
      }

      alert("Registration Successful!");
      return true;
    }
  </script>
</body>
</html>

5) React: Installation and Configuration. Design webpage for registration using React

npx create-react-app registration-app
cd registration-app
npm start

import React, { useState } from "react";

function App() {
  const [form, setForm] = useState({ name: "", email: "", pass: "" });

  function handleChange(e) {
    setForm({ ...form, [e.target.name]: e.target.value });
  }

  function handleSubmit(e) {
    e.preventDefault();
    alert(`Registered!\nName: ${form.name}\nEmail: ${form.email}`);
  }

  return (
    <div style={{ textAlign: "center", marginTop: "50px" }}>
      <h2>Registration Form</h2>
      <form onSubmit={handleSubmit}>
        <input name="name" placeholder="Name" onChange={handleChange} /><br/><br/>
        <input name="email" placeholder="Email" onChange={handleChange} /><br/><br/>
        <input name="pass" type="password" placeholder="Password" onChange={handleChange} /><br/><br/>
        <button type="submit">Register</button>
      </form>
    </div>
  );
}

export default App;


6) Node.Js: Installation and Configuration. Write a program for calculator using Node.

node -v
npm -v

mkdir node-calculator
cd node-calculator

// calculator.js

const readline = require("readline");

const rl = readline.createInterface({
  input: process.stdin,
  output: process.stdout
});

rl.question("Enter first number: ", (a) => {
  rl.question("Enter operator (+ - * /): ", (op) => {
    rl.question("Enter second number: ", (b) => {
      a = parseFloat(a);
      b = parseFloat(b);
      let result;

      if (op === "+") result = a + b;
      else if (op === "-") result = a - b;
      else if (op === "*") result = a * b;
      else if (op === "/") result = b !== 0 ? a / b : "Error (divide by 0)";
      else result = "Invalid operator";

      console.log(`Result: ${result}`);
      rl.close();
    });
  });
});


node calculator.js
